# Dependências da aplicação.
# Instalar com:  pip install -r requirements.txt

Django>=5.2,<6.0
dj-database-url>=2.1      # lê a configuração do banco da variável DATABASE_URL
whitenoise>=6.6           # serve arquivos estáticos sem servidor separado
psycopg[binary]>=3.1      # driver do PostgreSQL (produção)
gunicorn>=21.2            # servidor de aplicação (produção)
